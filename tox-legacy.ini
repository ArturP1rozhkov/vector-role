[tox]
minversion = 3.20
envlist = py36
skipsdist = true

[testenv]
basepython = python3.6
passenv = *
deps =
    -r tox-legacy-requirements.txt
commands =
    {posargs:molecule test -s podman --destroy always}