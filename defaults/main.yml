---
vector_version: "0.37.1"
vector_install_dir: "{{ ansible_facts['user_dir'] }}/vector"

vector_sources:
  demo_stdin:
    type: stdin

vector_sinks:
  demo_console:
    type: console
    inputs:
      - demo_stdin
    encoding:
      codec: text